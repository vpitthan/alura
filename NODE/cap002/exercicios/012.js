function imprimirIndicesElementos(arr) {
    for (let i = 0; i < arr.length; i++) {
        console.log(`Índice: ${i}, Elemento: ${arr[i]}`);
    }
}

// Exemplo de uso
const minhaArray = [10, 20, 30, 40];
imprimirIndicesElementos(minhaArray);
